-- Hardtime configuration (helps learn vim motions)

require('hardtime').setup({
  enabled = true,
  disable_mouse = false,
  hint = true,
  notification = true,
  allow_different_key = true,
  max_count = 3,
})
